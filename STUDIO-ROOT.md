# Playable Studio — корінь (ЧИТАЙ ПЕРШИМ, кожну сесію)

Це **фабрика плейблів**, а не окремі плейбли. Працюємо зафіксованим конвеєром і
контрактами. **Не фрілансимо** — не вигадуємо те, що вже напрацьовано.

Цей файл — короткий вказівник + HARD-правила. **Деталі процесу не тут — вони в одному
місці:** [`repo/docs/PIPELINE.md`](repo/docs/PIPELINE.md).

## 0. Найперше (структурна діра, через яку все ламалось)

Робоча тека сесії — цей корінь `E:/Work Stuff/playable/`, але виробництво живе в
`repo/`, і `repo/CLAUDE.md` авто-НЕ-вантажиться звідси. Тому перш ніж торкатись `repo/`:
1. `repo/docs/PIPELINE.md` — конвеєр (8 станцій, дві гілки, контракти).
2. `repo/docs/ANTIPATTERNS.md` — чого НЕ робити (читай і перед довгим дебагом).
3. `repo/CLAUDE.md` — kit hard-rules, BUILD-LOG, зонування.

## 1. Візія (навіщо) — `vision/`

Фабрика-не-плейбл; 4 шари (бренд / асети / механіка / наратив); універсальний рівень —
**наратив-кістяк**, не механіка; будуємо навколо **агностичної п'ятірки** (quiz, swipe,
reveal, match, choose). Головна вісь — **кінцева задача бізнесу**, механіка вторинна.

## 2. Де живе істина (канон — НЕ дублюй, посилайся)

| Що | Source of truth |
|---|---|
| **Процес (конвеєр)** | `repo/docs/PIPELINE.md` ← головне |
| Чого НЕ робити | `repo/docs/ANTIPATTERNS.md` |
| Компоненти UI | гілка A: `kit/kit.ts` · `docs/KIT.md` — гілка B: `kit/ui.ts` · `docs/GAME-UI.md` |
| Екрани / зони (A) | `repo/src/assetgen/layouts/` · `kit/layout.ts` |
| Lint якості (A) | `repo/docs/playable-design-rules.md` → `npm run check:layouts` |
| Асети (стиль) | `styles/<id>.brief.json`(A) / `styles/<id>.json`(B) · `brief.schema.json` |
| Дисципліна логу | `repo/CLAUDE.md` (BUILD-LOG + backlog) |

## 3. HARD non-negotiables (порушення = «фріланс», заборонено)

1. **Ревізія ПЕРЕД створенням.** Перш ніж робити компонент / меню / скіл / док — знайди
   наявний (kit, ui.ts, layouts, docs, skills, ANTIPATTERNS). Перевикористай або розшир.
   **Ніколи не плоди паралель.**
2. **UI = офіційний набір своєї гілки.** Гра (B) → `kit/ui.ts`; меню-демо (A) → `kit.ts`.
   **Ніколи не малюй UI примітивами Pixi і не заводь третій паралельний UI-модуль.**
3. **Тримайся конвеєра** (`PIPELINE.md`) — станція за станцією, з ✅ на кожній. Без ad-hoc.
4. **Гейт перед "готово".** Гра: `node tools/verify-lab.mjs <id>` зелений + BUILD-LOG.
   Меню: `check:layouts` + `visual`. Нема зелених гейтів → **не готово**.
5. **labs-first.** Нова гра → `node tools/new-lab.mjs <id>` → `labs/<id>/`; промоція в
   `templates/` — лише за стандартом + явним запитом.
6. **Сумніваєшся — читай канон / ANTIPATTERNS або питай. НЕ вигадуй.**
